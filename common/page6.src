#include "rom.h"
#include "macros.h"
#include "globals.h"

	* = $600

HDR(DEMO)
	CMN
	LDD(R0, PLS_1)
	LDD(R1, MNS_1)
	SVD(R1, SVD_T)
	LDD(R2, SVD_T)
	ESC
	BRK

BGN(FACTORIAL)
	SET(R1, 10.4456)
	SET(R2, 1)
	MOD(R3, R1, R2)
	SUB(R1, R1, R3)
_1	TST(R1)
	BRZ(_2)
	MUL(R2, R2, R1)
	DCR(R1)
	BRA(_1)
_2	EXT(S0)
	RTN
END(FACTORIAL)

END(DEMO)

SVD_T	.BYTE 0, 0, 0, 0
