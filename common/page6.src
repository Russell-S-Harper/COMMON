#include "rom.h"
#include "macros.h"
#include "globals.h"

	* = $600

HDR(DEMO)
	CMN
	LDD(R0, PLS_1)
	LDD(R1, MNS_1)
	ADD(R2, R0, R1)
	ESC
	BRK

BGN(FACTORIAL)
	SET(R1, 10.4456)
	SET(R2, 1)
	MOD(R3, R1, R2)
	SUB(R1, R1, R3)
_1	TST(R1)
	BRZ(_2)
	MUL(R2, R2, R1)
	DCR(R1)
	BRA(_1)
_2	EXT(S0)
	RTN
END(FACTORIAL)

END(DEMO)
